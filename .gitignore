# Termetron 完整开源仓库（服务端在根 + app/ Capacitor App 子目录）
#
# bin/ 存放 cloudflared 可执行文件（各平台 ~50MB）：不入库，
# 首次 `termetron remote on` 时自动下载（见 README）。
# app/ 内构建产物（node_modules/www/android）由 app/.gitignore 排除。

bin/
__pycache__/
*.pyc
